//import { tryConvert } from 'temperature';
const tryConvert = require('./temperature')


describe('zero degres', () => {
      describe('zero is 32', () => {
              it('marks the todo as completed', () => {
                tryConvert(10 , () =>{return "";});
                      });
            });
});
